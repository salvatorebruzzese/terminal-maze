#ifndef WILSON_H
#define WILSON_H

/**
 * Function that runs wilson's maze-generation algorithm.
 * @return an array of booleans of GAME_HEIGHT x GAME_WIDTH 
 * with the positions of the walls marked.
 */
bool **wilson();

#endif // WILSON_H